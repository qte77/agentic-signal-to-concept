# Scope — vertical-mode deep-dive from horizontal discovery

## Run scope

- **Problem-space / category**: agent/coding-agent memory and persistent-context tools — durable
  memory/context for AI coding agents (Claude Code, Cursor, Codex, etc.) across sessions, not a
  single product. Surfaced by `signal-discoverer`'s first real run
  (`discovery/2026-09-04T060617Z-categories.md` / `examples/2026-09-04T060617Z-discovery/`,
  category #1) — cross-source, ~18 distinct HN products + 4-5 on ProductHunt.
- **Slug**: agent-memory
- **Date window**: not bounded — default to `build-pattern-scanner`'s own 12-month default.

## Source inclusions/exclusions (optional)

v1 defaults: HN + ProductHunt for complaints; GitHub + Show HN for builds (Bluesky attempted per
spec, expect blocked). Reddit and app-store excluded per `docs/plans/0001-concept.md`.

## Query terms used (optional, fill in after a run)

-

## Independence heuristic override (optional)

None — apply `build-pattern-scanner.md`'s v1 default as-is.

## Notes carried from discovery

Look for what existing tools' users complain is still missing — cross-agent portability, review/audit
of stored memory, cost of storage/retrieval — rather than re-validating that the category exists (the
discovery pass already did that). Discovery's own HN product list for reference (not exhaustive, do
not treat as the ceiling of what's out there): MCP Memory, Mnemosyne, Remembrane, Engrava, Knowl,
Vibsync, GreatArrow.ai, Decispher, a public "Agent Memory Leaderboard." PH: Organizational Memory 2.0,
Sonnenfeld, Engram, MemHub.
